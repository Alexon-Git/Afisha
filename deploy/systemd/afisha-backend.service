[Unit]
Description=Afisha backend (docker-compose)
After=network.target

[Service]
Type=oneshot
WorkingDirectory=/var/www/alexon/Afisha/backend
ExecStart=/usr/bin/docker compose up -d --build
ExecStop=/usr/bin/docker compose down
RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
